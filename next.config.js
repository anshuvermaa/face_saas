/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
          '127.0.0.1', // Add localhost to the list of domains
        ],
    },
}

module.exports = nextConfig
